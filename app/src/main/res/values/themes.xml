<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FillInAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/fillin_black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:includeFontPadding">false</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen.IconBackground">
        <!--
          The animated icon to be displayed at the center of the splash screen.
          Note that the icon is *not* animated on API level 30 and below.
        -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!-- The background color behind the icon. -->
        <item name="windowSplashScreenIconBackgroundColor">@color/fillin_black</item>
        <!-- The background color for the splash screen. -->
        <item name="windowSplashScreenBackground">@color/fillin_black</item>
        <!-- The duration, in milliseconds, of the icon animation of the splash screen. -->
        <item name="windowSplashScreenAnimationDuration">2500</item>
        <!-- The 'core-splashscreen' switches the theme back to this after splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.FillInAndroid</item>
    </style>
</resources>